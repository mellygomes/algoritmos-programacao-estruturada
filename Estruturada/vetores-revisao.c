#include <stdio.h>

    int i, vetor[5]={1,2};   

int main(void) {

    //atribuir valoresna mesma linha   
    for (i=0; i<5; i++) {
        printf("%i\n", vetor[i]);
    }

    return 0;
}
